{
  "name": "parcel-tracker-server",
  "version": "1.0.0",
  "description": "Server for tracking parcel movement via Playwright",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install --with-deps"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.4",
    "playwright": "^1.44.0"
  }
}
